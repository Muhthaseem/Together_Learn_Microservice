apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa-service
  namespace: togetherlearn
spec:
  replicas: 2
  selector:
    matchLabels:
      app: qa-service
  template:
    metadata:
      labels:
        app: qa-service
    spec:
      containers:
        - name: qa-service
          image: togetherlearn/qa-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: tl-config
          env:
            - name: DB_URL
              value: jdbc:postgresql://postgres-qa:5432/qa_db
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tl-secrets
                  key: db-password
            - name: RABBITMQ_URI
              valueFrom:
                secretKeyRef:
                  name: tl-secrets
                  key: rabbitmq-uri
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1Gi
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: qa-service
  namespace: togetherlearn
spec:
  selector:
    app: qa-service
  ports:
    - port: 8084
      targetPort: 8084
